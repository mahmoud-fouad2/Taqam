# Expo public env vars must be prefixed with EXPO_PUBLIC_
# Use the live backend for release builds.
# For local device testing against your machine, replace this with your LAN IP.

EXPO_PUBLIC_API_BASE_URL="https://taqam.net"
