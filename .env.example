# =============================================
# Taqam HR Platform - Environment Variables
# =============================================
# Copy this file to .env and fill in the values

# ===== Database (External PostgreSQL / Neon / Managed Postgres) =====
# Use your pooled connection string for the runtime app.
# Prefer an explicit SSL mode from your provider. For Neon/managed Postgres, `sslmode=verify-full`
# preserves the current secure behavior and avoids upcoming pg/libpq compatibility warnings.
DATABASE_URL="postgresql://user:password@host:5432/taqam?sslmode=verify-full"
# Optional direct connection for migrations if your provider gives one.
DIRECT_URL="postgresql://user:password@host:5432/taqam?sslmode=verify-full"

# ===== Cloudflare R2 Storage =====
# Used for avatars, documents, and public career resume uploads.
R2_ACCOUNT_ID=""
R2_ACCESS_KEY_ID=""
R2_SECRET_ACCESS_KEY=""
R2_BUCKET_NAME="taqam"
R2_PUBLIC_URL="https://pub-408c0f665b964f47bcd1abfe89ac8aed.r2.dev"
R2_ENDPOINT="https://de95c4f37b252fdb5c22a69ed3d7d3a1.r2.cloudflarestorage.com"

# ===== Authentication (NextAuth) =====
# Generate with: openssl rand -base64 32
NEXTAUTH_SECRET=""
NEXTAUTH_URL="http://localhost:3000"

# ===== Mobile Authentication (Bearer JWT + Refresh) =====
# Access token signing secret (generate with: openssl rand -base64 32)
MOBILE_JWT_SECRET=""

# Refresh token hashing secret (generate with: openssl rand -base64 32)
MOBILE_REFRESH_TOKEN_SECRET=""

# Optional
# MOBILE_REFRESH_TOKEN_TTL_DAYS="30"
# MOBILE_CHALLENGE_TTL_SECONDS="120"

# ===== Google reCAPTCHA =====
NEXT_PUBLIC_RECAPTCHA_SITE_KEY=""
RECAPTCHA_SECRET_KEY=""

# ===== Error Tracking (Sentry) =====
# Create a project at https://sentry.io and paste the DSN(s).
SENTRY_DSN=""
NEXT_PUBLIC_SENTRY_DSN=""

# Optional: For uploading source maps
SENTRY_ORG=""
SENTRY_PROJECT=""
SENTRY_AUTH_TOKEN=""

# ===== Logging Configuration =====
# Log level: debug | info | warn | error
# Use 'info' in production, 'debug' in development
LOG_LEVEL="info"

# Enable/disable audit logging (default: true)
# Set to 'false' to disable automatic audit logging via Prisma middleware
ENABLE_AUDIT_LOGGING="true"

# ===== Application =====
NEXT_PUBLIC_APP_URL="http://localhost:3000"
NEXT_PUBLIC_APP_NAME="Taqam"
NEXT_PUBLIC_DEFAULT_LOCALE="ar"

# ===== Marketing / SEO (Optional) =====
# Used for JSON-LD AggregateRating (only set if true)
NEXT_PUBLIC_RATING_VALUE="5"
NEXT_PUBLIC_RATING_COUNT="123"

# Used for canonical/OG URLs (fallbacks to https://taqam.net)
NEXT_PUBLIC_SITE_URL="http://localhost:3000"

# ===== Base Domain (for multi-tenant subdomains) =====
TAQAM_BASE_DOMAIN="taqam.net"

# ===== Support (Optional) =====
# Used on guest support page
# NEXT_PUBLIC_SUPPORT_EMAIL="support@taqam.net"

# ===== Email (Optional - for notifications) =====
# SMTP_HOST=""
# SMTP_PORT="587"
# SMTP_USER=""
# SMTP_PASSWORD=""
# SMTP_FROM="noreply@taqam.net"
# MAIL_FROM_FALLBACK="noreply@taqam.net"

# ===== Distributed Rate Limiting (Optional but recommended in production) =====
# Enables shared rate limiting across all app instances.
# When unset, the app falls back to per-instance in-memory limits.
# UPSTASH_REDIS_REST_URL=""
# UPSTASH_REDIS_REST_TOKEN=""

# ===== Super Admin (First Setup) =====
# Used by seed script to create initial super admin
SUPER_ADMIN_EMAIL="admin@taqam.net"
SUPER_ADMIN_PASSWORD=""

# Force bootstrap admin reset/ensure (used by scripts/ensure-super-admin.mjs)
# SUPER_ADMIN_FORCE="false"

# ===== Render Bootstrap (Optional) =====
# Retry prisma db push on Render before starting the app
# DB_BOOTSTRAP_ATTEMPTS="10"
# DB_BOOTSTRAP_DELAY_MS="3000"

# ===== Smoke Tests (Optional) =====
# Used by: pnpm smoke
# SMOKE_BASE_URL="http://localhost:3000"
# SMOKE_TIMEOUT_MS="10000"

